---
layout: base.njk
---
    <article>
      <div class="headline">
        <h1>{{ title }}</h1>
        <time datetime="{{ page.date | isoDate }}">{{ page.date | readableDate }}</time>
      </div>

{{ content | safe }}
    </article>
